Trello.Routers.AppRouter = Backbone.Router.extend({
  initialize: function ($rootEl){
    this.$rootEl = $rootEl;
    // this.collection
  },

  routes: {
    '': 'index'
  },

  index: function () {
    
  }
})
